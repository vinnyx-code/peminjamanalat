@extends('layouts.app')
@section('title', $userData->exists ? 'Edit User' : 'Tambah User')
@section('content')
<div class="mb-4"><a href="{{ route('admin.users.index') }}" class="small text-decoration-none"><i class="bi bi-arrow-left me-1"></i>Kembali</a><h2 class="fw-bold mt-3">{{ $userData->exists ? 'Edit User' : 'Tambah User' }}</h2></div>
<div class="card p-3 p-md-4" style="max-width:720px"><form method="POST" action="{{ $userData->exists ? route('admin.users.update',$userData) : route('admin.users.store') }}">@csrf @if($userData->exists) @method('PUT') @endif
<div class="row g-3"><div class="col-md-6"><label class="form-label">Nama lengkap</label><input name="nama" value="{{ old('nama',$userData->nama ?: $userData->name) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">Username</label><input name="username" value="{{ old('username',$userData->username) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">Email</label><input type="email" name="email" value="{{ old('email',$userData->email) }}" class="form-control" required></div><div class="col-md-6"><label class="form-label">Role</label><select name="role" class="form-select">@foreach(['admin','petugas','peminjam'] as $role)<option value="{{ $role }}" @selected(old('role',$userData->role ?: 'peminjam')===$role)>{{ ucfirst($role) }}</option>@endforeach</select></div><div class="col-12"><label class="form-label">Password {{ $userData->exists ? '(kosongkan jika tidak diubah)' : '' }}</label><input type="password" name="password" class="form-control" {{ $userData->exists ? '' : 'required' }}></div></div><button class="btn btn-primary mt-4">Simpan</button> <a href="{{ route('admin.users.index') }}" class="btn btn-light mt-4">Batal</a></form></div>
@endsection
